% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/artificial_features.R
\name{make_artificial_features}
\alias{make_artificial_features}
\title{Dispatcher for Artificial Feature Generation}
\usage{
make_artificial_features(x, n_injected, type, random_state = NULL)
}
\arguments{
\item{x}{Numeric matrix of predictors (samples \eqn{\times} features).
Must have more columns than \code{n_injected} for random permutation; for
knockoffs, a fallback to random permutation is attempted when the
knockoff constructor fails (e.g., rank-deficient input). Fixed-X
knockoffs that require row augmentation also fall back because augmented
knockoff rows cannot be aligned with the original outcome. All knockoff
modes fail closed above 3 000 predictors; random permutation has no such
dimensional limit.}

\item{n_injected}{Positive integer; number of artificial columns to append.
Typically \code{round(ncol(x) * artificial_proportion)} as computed in
\code{\link[=stabl_fit]{stabl_fit()}}.}

\item{type}{Character string; one of \code{"random_permutation"}, \code{"knockoff"},
\code{"knockoff_equi"}, or \code{"knockoff_mvr"}.}

\item{random_state}{Optional integer; passed to \code{\link[=set.seed]{set.seed()}} before any
random operations for reproducibility.  \code{NULL} leaves the RNG unchanged.
Seeding happens exactly once in this dispatcher; downstream generators
inherit the seeded RNG state and do not re-seed (audit M-5).}
}
\value{
Named list with three elements:
\describe{
\item{\code{x_augmented}}{Numeric matrix of size
(nrow(x)) \eqn{\times} (ncol(x) + n_injected) with the artificial
columns appended after the original features.}
\item{\code{noise_col_indices}}{Integer vector of length \code{n_injected}
containing the 1-based indices into the \strong{original} \code{x} columns
(not into the artificial block) that identify which source features
were used to build each artificial column.  Used by \code{\link[=stabl_fit]{stabl_fit()}}
to look up sparse-group-lasso group memberships for the artificial
block via \code{.append_noise_groups}.}
\item{\code{artificial_provenance}}{Additive metadata describing the
requested artificial-feature type, \code{actual_type} used for scientific
classification, all generator modes used by the selected artificial
columns, and the ordered structured \code{fallback_history}. Per-chunk
counts and reasons remain available for 0.1.x compatibility.}
}
}
\description{
Selects and calls the appropriate artificial-feature generator based on
\code{type}, returning the augmented predictor matrix together with the column
indices of the injected noise block.
}
\details{
Injecting artificial features supports STABL's FDP+ diagnostic calibration:
by mixing known-noise columns into the predictor matrix alongside real
features, STABL can empirically estimate how often a variable of pure noise
is selected at a given stability threshold.  This observed noise-selection
rate drives the FDP+ estimate computed in \code{\link[=compute_fdp_plus]{compute_fdp_plus()}}. The current
rule chooses its minimum and does not imply universal error-rate control.

Four noise strategies are supported:
\describe{
\item{\code{"random_permutation"}}{Copies \code{n_injected} randomly chosen real
columns and shuffles each copy independently, breaking all signal while
preserving marginal distributions.  Fast and broadly applicable.}
\item{\code{"knockoff"}}{Generates \strong{fixed-X} knockoffs via
\code{\link[knockoff:create.fixed]{knockoff::create.fixed()}}, which preserve the covariance structure of
the original features under the fixed-design assumption.  Kept for
backward compatibility.}
\item{\code{"knockoff_equi"}}{Generates \strong{model-X equicorrelated} knockoffs
via \code{knockoff::create.gaussian(..., method = "equi")}.  This matches the
\code{GaussianSampler(method='equicorrelated')} call in Python STABL and is
the parity-correct knockoff type for cross-language comparisons.}
\item{\code{"knockoff_mvr"}}{Generates \strong{model-X MVR} (minimum-variance-
reconstructability) knockoffs.  The S-matrix is solved by \code{solve_mvr()}
(an R-native port of \code{knockpy.mrc}); sampling uses
\code{knockoff::create.gaussian(..., diag_s = S)}.  This is a novel feature
exclusive to \code{stablr}.}
}
}
\examples{
set.seed(1L)
x <- matrix(
  rnorm(20 * 4), 20, 4,
  dimnames = list(paste0("s", 1:20), paste0("f", 1:4))
)
generated <- make_artificial_features(
  x, n_injected = 2L, type = "random_permutation", random_state = 1L
)
dim(generated$x_augmented)
generated$artificial_provenance$actual_type
try(make_artificial_features(x, 0L, "random_permutation"))

}
\seealso{
\code{\link[=compute_fdp_plus]{compute_fdp_plus()}} which consumes the artificial-feature scores,
\code{\link[=stabl_fit]{stabl_fit()}} which calls this function automatically.
}
