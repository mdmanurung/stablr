% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/multiomic_workflows.R, R/stabl_accessors.R
\name{stabl_multiomic_train_validate}
\alias{stabl_multiomic_train_validate}
\alias{print.stabl_multiomic_fit}
\title{Multi-Omic STABL Train/Validation Workflow}
\usage{
stabl_multiomic_train_validate(
  x_train_list,
  y_train,
  lambda_grid,
  x_valid_list = NULL,
  y_valid = NULL,
  groups_train = NULL,
  base_learner = "lasso",
  family = "gaussian",
  n_bootstraps = 100L,
  artificial_type = "random_permutation",
  hard_threshold = NULL,
  stratify_bootstrap = FALSE,
  bootstrap_strata_train = NULL,
  l1_ratio = NULL,
  random_state = NULL,
  early_fusion = FALSE,
  late_fusion = FALSE,
  late_fusion_training = c("oof", "python_legacy"),
  late_fusion_nfolds = 5L,
  n_iter_lf = 10000L,
  cooperative_fusion = FALSE,
  rho = NULL,
  cooperation_selection = c("cv", "validation"),
  cooperation_selector = c("lambda.min", "lambda.1se"),
  cooperation_type_measure = "default",
  cooperation_nfolds = 5L,
  ...
)

\method{print}{stabl_multiomic_fit}(x, ...)
}
\arguments{
\item{x_train_list}{Named list of training omic tables (\code{data.frame} or
numeric matrix), each with row names as sample IDs.}

\item{y_train}{Named outcome vector for training samples.}

\item{lambda_grid}{Either a shared lambda grid (\code{data.frame} or \code{"auto"})
used for all omics, or a named list mapping each omic name to its own
lambda grid.}

\item{x_valid_list}{Optional named list of validation omic tables. When
supplied, names must match \code{x_train_list} names.}

\item{y_valid}{Optional named outcome vector for validation samples. When
supplied together with \code{x_valid_list}, sample alignment is validated.}

\item{groups_train}{Optional named grouping vector for training samples.
When supplied, grouped bootstrap sampling is used in each per-omic fit.}

\item{base_learner}{Passed to \code{\link[=stabl_fit]{stabl_fit()}}.}

\item{family}{Character; \code{glmnet} response family (\code{"gaussian"},
\code{"binomial"}, \code{"multinomial"}, or \code{"cox"}). Default: \code{"gaussian"}.}

\item{n_bootstraps}{Passed to \code{\link[=stabl_fit]{stabl_fit()}}.}

\item{artificial_type}{Passed to \code{\link[=stabl_fit]{stabl_fit()}}.}

\item{hard_threshold}{Passed to \code{\link[=stabl_fit]{stabl_fit()}}.}

\item{stratify_bootstrap}{Passed to \code{\link[=stabl_fit]{stabl_fit()}}. When \code{TRUE}, per-omic
and early-fusion STABL fits draw class-stratified bootstrap subsamples.}

\item{bootstrap_strata_train}{Optional categorical bootstrap stratification
design for training samples, forwarded to \code{\link[=stabl_fit]{stabl_fit()}}.}

\item{l1_ratio}{Passed to \code{\link[=stabl_fit]{stabl_fit()}} when \code{lambda_grid = "auto"}.
Use this with \code{base_learner = "elastic_net"} to generate alpha-aware auto
grids.}

\item{random_state}{Integer or \code{NULL}; top-level seed.  Default: \code{NULL}.}

\item{early_fusion}{Logical.  When \code{TRUE}, a single \code{\link[=stabl_fit]{stabl_fit()}} is run on
the column-bound concatenation of all omic matrices in addition to the
per-omic fits.  Results are returned in the \code{early_fusion} field.}

\item{late_fusion}{Logical.  When \code{TRUE}, a downstream predictor is fitted
per omic on its selected features, and \code{\link[=stacked_multi_omic]{stacked_multi_omic()}} combines the
per-omic predictions.  If no features are selected for an omic, late
fusion falls back to class priors for multinomial tasks or the train-set
mean for other tasks. The \code{task_type} (binary, regression, or multiclass)
is inferred from \code{family}.  Results are returned in the \code{late_fusion}
field.}

\item{late_fusion_training}{How stacking weights are trained. \code{"oof"}
(default) refits STABL selection and the downstream model inside every
stacking fold. \code{"python_legacy"} preserves the historical in-sample
Python-compatible algorithm.}

\item{late_fusion_nfolds}{Number of stacking folds used in OOF mode.}

\item{n_iter_lf}{Number of random weight draws passed to
\code{\link[=stacked_multi_omic]{stacked_multi_omic()}} during late fusion.  Ignored when
\code{late_fusion = FALSE}.}

\item{cooperative_fusion}{Logical. When \code{TRUE}, fit a built-in cooperative
learning branch (vendored multiview engine) in addition to the existing
per-omic STABL fits. Native v1 supports \code{family = "gaussian"} and
\code{"binomial"} only.}

\item{rho}{Numeric scalar or vector of non-negative cooperation strengths.
When \code{NULL}, defaults to \code{0}.}

\item{cooperation_selection}{Character scalar. Either \code{"cv"} or
\code{"validation"}. \code{"cv"} tunes over \code{rho} with shared inner fold
assignments. \code{"validation"} tunes over \code{rho} and \code{lambda} on the
supplied validation set.}

\item{cooperation_selector}{Character scalar. Selection rule for the
cooperative \code{lambda}. \code{"lambda.1se"} is only available when
\code{cooperation_selection = "cv"}.}

\item{cooperation_type_measure}{Character scalar controlling the cooperative
tuning metric. Supported values follow the active \code{family} and the
multiview CV API.}

\item{cooperation_nfolds}{Number of inner folds used when
\code{cooperation_selection = "cv"}.}

\item{...}{Additional arguments forwarded to \code{\link[=stabl_fit]{stabl_fit()}}.}

\item{x}{A numeric matrix or \code{data.frame} (samples \eqn{\times} features)
with row names used as sample IDs.}
}
\value{
A named list with class \code{"stabl_multiomic_fit"} containing:
\describe{
\item{\code{fits}}{Named list of per-omic \code{stabl_fit} objects.}
\item{\code{selected_features}}{Named list of selected feature names.}
\item{\code{selected_train}}{Named list of training matrices restricted to
selected features (possibly 0-column).}
\item{\code{selected_valid}}{Named list of validation matrices restricted to
selected features, or \code{NULL} when no validation input is provided.}
\item{\code{early_fusion}}{\code{NULL} when \code{early_fusion = FALSE}.  Otherwise a
list with \code{fit}, \code{selected_features}, \code{selected_train}, and
\code{selected_valid} for the concatenated single-STABL run.}
\item{\code{late_fusion}}{\code{NULL} when \code{late_fusion = FALSE}.  Otherwise a
list with \code{weights}, \code{valid_predictions}, and mode-specific training
results. OOF mode provides \code{oof_predictions} and \code{oof_score}; legacy
mode provides \code{in_sample_predictions} and \code{in_sample_score}.
\code{train_predictions} and \code{score} remain exact 0.1.x compatibility
aliases for the active mode-specific fields. Multinomial tasks also
include \code{levels}, \code{log_loss}, and classification metrics.}
\item{\code{cooperative_fusion}}{Present only when
\code{cooperative_fusion = TRUE}. A list containing the selected multiview
fit, chosen \code{rho} and \code{lambda}, selected features per view,
train/validation predictions, and tuning diagnostics.}
}
}
\description{
Fits \code{\link[=stabl_fit]{stabl_fit()}} independently on each omic block from a named list,
then returns per-omic fitted objects and selected-feature matrices for
downstream composition. Optional early-fusion, late-fusion, and
cooperative-fusion branches are additive to the per-omic STABL results.
}
\section{Functions}{
\itemize{
\item \code{print(stabl_multiomic_fit)}: Print a concise summary of a
\code{stabl_multiomic_fit} object; invisibly returns \code{x}.

}}
\examples{
set.seed(1L)
ids <- paste0("s", 1:12)
x_list <- list(
  rna = matrix(rnorm(12 * 4), 12, 4,
               dimnames = list(ids, paste0("g", 1:4))),
  protein = matrix(rnorm(12 * 3), 12, 3,
                   dimnames = list(ids, paste0("p", 1:3)))
)
y <- setNames(rnorm(12), ids)
fit <- stabl_multiomic_train_validate(
  x_list, y, lambda_grid = data.frame(lambda = c(0.2, 0.1)),
  n_bootstraps = 2L, artificial_type = NULL, hard_threshold = 0.5,
  random_state = 1L
)
names(fit$fits)
}
