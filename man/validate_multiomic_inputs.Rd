% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/input_validation.R
\name{validate_multiomic_inputs}
\alias{validate_multiomic_inputs}
\title{Validate Multi-Omic Input Contract}
\usage{
validate_multiomic_inputs(x_list, y, groups = NULL)
}
\arguments{
\item{x_list}{Named list of \code{data.frame} or numeric matrix omic tables.
Each table must have non-empty row names identifying samples.  All tables
must have the same set of row names \strong{in the same order}.}

\item{y}{Named outcome vector aligned to the samples in \code{x_list}.  The
name set must match the row names of every omic table.}

\item{groups}{Optional named group vector.  When supplied its names must
match the sample IDs in \code{x_list}.  Pass \code{NULL} to skip group validation.}
}
\value{
Invisibly returns \code{TRUE} when all checks pass.  Raises an
informative error as soon as the first violation is found.
}
\description{
Enforces the canonical \code{stablr} input contract for multi-omic analyses: a
named list of omic tables with identical sample IDs and row order across
all views, plus strict alignment with the outcome and optional group
vectors.
}
\details{
Multi-omic analyses require that all omic matrices have exactly the same
rows in exactly the same order so that row-wise operations (bootstrap
sampling, cooperative learning) are coherent.  This function catches
mismatches early and provides informative error messages naming the
offending omic layer, which is much easier to diagnose than silent
misalignment detected later in the modelling pipeline.
}
\examples{
ids <- paste0("s", 1:4)
x_list <- list(
  rna = matrix(1:12, 4, 3, dimnames = list(ids, paste0("g", 1:3))),
  protein = matrix(1:8, 4, 2, dimnames = list(ids, paste0("p", 1:2)))
)
y <- setNames(c(0, 1, 0, 1), ids)
validate_multiomic_inputs(x_list, y)
try(validate_multiomic_inputs(x_list[c("rna", "rna")], y))

}
\seealso{
\code{\link[=validate_sample_alignment]{validate_sample_alignment()}} for single-omic inputs,
\code{\link[=stabl_multiomic_train_validate]{stabl_multiomic_train_validate()}} which calls this automatically.
}
