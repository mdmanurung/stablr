% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/multiomic_workflows.R, R/stabl_accessors.R
\name{stabl_multiomic_cv}
\alias{stabl_multiomic_cv}
\alias{print.stabl_multiomic_cv}
\title{Multi-Omic STABL Cross-Validation Workflow}
\usage{
stabl_multiomic_cv(
  x_list,
  y,
  lambda_grid,
  v = 5L,
  groups = NULL,
  base_learner = "lasso",
  family = "gaussian",
  n_bootstraps = 100L,
  artificial_type = "random_permutation",
  hard_threshold = NULL,
  stratify_bootstrap = FALSE,
  bootstrap_strata = NULL,
  l1_ratio = NULL,
  random_state = NULL,
  early_fusion = FALSE,
  late_fusion = FALSE,
  late_fusion_training = c("oof", "python_legacy"),
  late_fusion_nfolds = 5L,
  n_iter_lf = 10000L,
  cooperative_fusion = FALSE,
  rho = NULL,
  cooperation_selection = c("cv", "validation"),
  cooperation_selector = c("lambda.min", "lambda.1se"),
  cooperation_type_measure = "default",
  cooperation_nfolds = 5L,
  ...
)

\method{print}{stabl_multiomic_cv}(x, ...)
}
\arguments{
\item{x_list}{Named list of omic tables (\code{data.frame} or numeric matrix),
each with row names as sample IDs.}

\item{y}{Named outcome vector.}

\item{lambda_grid}{Either a shared lambda grid (\code{data.frame} or \code{"auto"})
used for all omics, or a named list mapping each omic name to its own
lambda grid.}

\item{v}{Number of folds. Must be at least 2.}

\item{groups}{Optional named grouping vector. When supplied, all samples in
the same group are assigned to the same assessment fold.}

\item{base_learner}{Passed to \code{\link[=stabl_fit]{stabl_fit()}}.}

\item{family}{Character; \code{glmnet} response family (\code{"gaussian"},
\code{"binomial"}, \code{"multinomial"}, or \code{"cox"}). Default: \code{"gaussian"}.}

\item{n_bootstraps}{Passed to \code{\link[=stabl_fit]{stabl_fit()}}.}

\item{artificial_type}{Passed to \code{\link[=stabl_fit]{stabl_fit()}}.}

\item{hard_threshold}{Passed to \code{\link[=stabl_fit]{stabl_fit()}}.}

\item{stratify_bootstrap}{Passed to \code{\link[=stabl_fit]{stabl_fit()}}.}

\item{bootstrap_strata}{Optional categorical bootstrap stratification design
forwarded to \code{\link[=stabl_fit]{stabl_fit()}} for each training fold.}

\item{l1_ratio}{Passed to \code{\link[=stabl_fit]{stabl_fit()}} when \code{lambda_grid = "auto"}.}

\item{random_state}{Optional integer seed used for deterministic fold
assignment and forwarded to each per-fold \code{\link[=stabl_fit]{stabl_fit()}} call.}

\item{early_fusion}{Logical.  Forwarded to each per-fold
\code{\link[=stabl_multiomic_train_validate]{stabl_multiomic_train_validate()}} call.}

\item{late_fusion}{Logical.  Forwarded to each per-fold
\code{\link[=stabl_multiomic_train_validate]{stabl_multiomic_train_validate()}} call.}

\item{late_fusion_training}{Forwarded to
\code{\link[=stabl_multiomic_train_validate]{stabl_multiomic_train_validate()}}.}

\item{late_fusion_nfolds}{Forwarded to
\code{\link[=stabl_multiomic_train_validate]{stabl_multiomic_train_validate()}}.}

\item{n_iter_lf}{Forwarded to \code{\link[=stabl_multiomic_train_validate]{stabl_multiomic_train_validate()}}.}

\item{cooperative_fusion}{Forwarded to
\code{\link[=stabl_multiomic_train_validate]{stabl_multiomic_train_validate()}}.}

\item{rho}{Forwarded to \code{\link[=stabl_multiomic_train_validate]{stabl_multiomic_train_validate()}}.}

\item{cooperation_selection}{Forwarded to
\code{\link[=stabl_multiomic_train_validate]{stabl_multiomic_train_validate()}}.}

\item{cooperation_selector}{Forwarded to
\code{\link[=stabl_multiomic_train_validate]{stabl_multiomic_train_validate()}}.}

\item{cooperation_type_measure}{Forwarded to
\code{\link[=stabl_multiomic_train_validate]{stabl_multiomic_train_validate()}}.}

\item{cooperation_nfolds}{Forwarded to
\code{\link[=stabl_multiomic_train_validate]{stabl_multiomic_train_validate()}}.}

\item{...}{Additional arguments forwarded to \code{\link[=stabl_fit]{stabl_fit()}}.}

\item{x}{A numeric matrix or \code{data.frame} (samples \eqn{\times} features)
with row names used as sample IDs.}
}
\value{
A list with class \code{"stabl_multiomic_cv"} containing:
\describe{
\item{\code{folds}}{List of fold descriptors with \code{fold}, \code{train_ids}, and
\code{valid_ids}.}
\item{\code{fold_results}}{Named list of per-fold
\code{stabl_multiomic_fit} results.}
\item{\code{diagnostics}}{Data frame with one row per fold/omic and columns
\code{fold}, \code{omic}, \code{n_selected}, \code{threshold}, and \code{max_score}.}
}
}
\description{
Builds deterministic fold splits over a named multi-omic input, fits
\code{\link[=stabl_multiomic_train_validate]{stabl_multiomic_train_validate()}} on each training fold, and returns
fold-wise selection diagnostics together with selected train/validation
matrices for downstream inspection.
}
\details{
Optional early-fusion, late-fusion, and cooperative-fusion branches are
forwarded to each fold-specific \code{\link[=stabl_multiomic_train_validate]{stabl_multiomic_train_validate()}} call.
}
\section{Methods (by generic)}{
\itemize{
\item \code{print(stabl_multiomic_cv)}: Print a concise summary of a
\code{stabl_multiomic_cv} object; invisibly returns \code{x}.

}}
\examples{
set.seed(2L)
ids <- paste0("s", 1:12)
x_list <- list(
  rna = matrix(rnorm(12 * 4), 12, 4,
               dimnames = list(ids, paste0("g", 1:4))),
  protein = matrix(rnorm(12 * 3), 12, 3,
                   dimnames = list(ids, paste0("p", 1:3)))
)
y <- setNames(rnorm(12), ids)
cv <- stabl_multiomic_cv(
  x_list, y, lambda_grid = data.frame(lambda = 0.1), v = 2L,
  n_bootstraps = 2L, artificial_type = NULL, hard_threshold = 0.5,
  random_state = 2L
)
cv$diagnostics
}
