% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/stabl_fit.R, R/stabl_accessors.R
\name{stabl_fit}
\alias{stabl_fit}
\alias{print.stabl_fit}
\title{Fit STABL (Stability-Penalized Feature Selection)}
\usage{
stabl_fit(
  x,
  y,
  lambda_grid,
  base_learner = "lasso",
  family = "gaussian",
  n_bootstraps = 1000L,
  artificial_type = "random_permutation",
  artificial_proportion = 1,
  sample_fraction = 0.5,
  replace = FALSE,
  hard_threshold = NULL,
  fdr_threshold_range = seq(0, 0.99, by = 0.01),
  explore = FALSE,
  n_explore = 5L,
  groups = NULL,
  stratify_bootstrap = FALSE,
  bootstrap_strata = NULL,
  n_lambda = 30L,
  l1_ratio = NULL,
  verbose = FALSE,
  workers = 1L,
  random_state = NULL,
  adaptive_gamma = 1,
  adaptive_epsilon = 1e-06,
  feature_groups = NULL,
  corr_group_threshold = NULL
)

\method{print}{stabl_fit}(x, ...)
}
\arguments{
\item{x}{A numeric matrix or \code{data.frame} (samples \eqn{\times} features)
with row names used as sample IDs.}

\item{y}{A named numeric/factor vector whose names are sample IDs, or a
matrix-like outcome (for example \code{survival::Surv}) with row names as
sample IDs.}

\item{lambda_grid}{A pre-expanded \code{data.frame} (each row = one parameter
combination) with at least a \code{lambda} column.  Pass \code{"auto"} to derive a
data-driven grid via \code{\link[=auto_lambda_grid]{auto_lambda_grid()}} (uses \code{family} and \code{n_lambda}).}

\item{base_learner}{Character; \code{"lasso"} (alpha = 1), \code{"elastic_net"}
(reads \code{alpha} from the \code{lambda_grid} \code{alpha} column), or
\code{"adaptive_lasso"}, or \code{"sparse_group_lasso"}. Default: \code{"lasso"}.}

\item{family}{Character; \code{glmnet} response family (\code{"gaussian"},
\code{"binomial"}, \code{"multinomial"}, or \code{"cox"}). Default: \code{"gaussian"}.}

\item{n_bootstraps}{Positive integer; bootstrap iterations per lambda.
Default: \code{1000L}.}

\item{artificial_type}{Character or \code{NULL}; \code{"random_permutation"},
\code{"knockoff"}, \code{"knockoff_equi"}, \code{"knockoff_mvr"}, or \code{NULL} (no
artificial features — requires \code{hard_threshold}).  Default:
\code{"random_permutation"}.}

\item{artificial_proportion}{Numeric in \verb{(0, 1]}; fraction of original
features to inject as artificial noise.  Default: \code{1.0}.}

\item{sample_fraction}{Positive numeric; fraction of samples drawn per
bootstrap.  Default: \code{0.5}.}

\item{replace}{Logical; sample with replacement?  Default: \code{FALSE}.}

\item{hard_threshold}{Numeric in \verb{(0, 1]} or \code{NULL}.  When supplied, FDP+
control is bypassed and this value is the stability-score cut-off.
Default: \code{NULL}.}

\item{fdr_threshold_range}{Numeric vector swept when computing FDP+.
Default: \code{seq(0, 0.99, by = 0.01)}, matching Python STABL's
\code{np.arange(0., 1., .01)}.}

\item{explore}{Logical; if \code{TRUE} and no features pass the threshold, fall
back to the top \code{n_explore} features.  Default: \code{FALSE}.}

\item{n_explore}{Positive integer; fallback feature count.  Default: \code{5L}.}

\item{groups}{Named vector of group IDs (same names as \code{rownames(x)}) or
\code{NULL}.  When supplied, \code{\link[=group_bootstrap_indices]{group_bootstrap_indices()}} is used instead of
\code{\link[=classic_bootstrap_indices]{classic_bootstrap_indices()}}.  Default: \code{NULL}.}

\item{stratify_bootstrap}{Logical; if \code{TRUE}, bootstrap subsamples are
stratified by the outcome class.  This is intended for classification
tasks with small or imbalanced classes.  Default \code{FALSE} preserves the
original STABL sampling behavior.}

\item{bootstrap_strata}{Optional categorical stratification design for
bootstrap sampling.  Provide a named vector, matrix, \code{data.frame}, or
list with one row/value per sample.  Multiple columns are combined as a
joint interaction stratum, for example outcome class by study group.  When
supplied, this overrides \code{stratify_bootstrap}.}

\item{n_lambda}{Integer; number of lambda values when \code{lambda_grid = "auto"}.
Ignored otherwise.  Default: \code{30L}.}

\item{l1_ratio}{Numeric scalar, numeric vector, or \code{NULL}. Passed to
\code{\link[=auto_lambda_grid]{auto_lambda_grid()}} when \code{lambda_grid = "auto"}. Use this for
\code{base_learner = "elastic_net"} so the generated grid contains the
elastic-net \code{alpha} column. Default \code{NULL} preserves the lasso-like auto
grid used by existing workflows.}

\item{verbose}{Logical; emit progress messages.  Default: \code{FALSE}.}

\item{workers}{Positive integer; parallel workers for
\code{furrr::future_map()}.  Actual parallelism requires the caller to invoke
\code{future::plan(multisession, workers = N)} first.  Default: \code{1L}.}

\item{random_state}{Integer or \code{NULL}; top-level seed.  Default: \code{NULL}.}

\item{adaptive_gamma}{Positive numeric scalar. Used only when
\code{base_learner = "adaptive_lasso"}.}

\item{adaptive_epsilon}{Positive numeric scalar. Used only when
\code{base_learner = "adaptive_lasso"}.}

\item{feature_groups}{Optional feature-group definition for
\code{base_learner = "sparse_group_lasso"}. Provide a vector/factor of length
\code{ncol(x)} assigning each original feature to a group.}

\item{corr_group_threshold}{Optional numeric percentile in \verb{(0, 100]} used
to derive sparse-group feature groups from pairwise correlations of
original features. Used only when
\code{base_learner = "sparse_group_lasso"}.}

\item{...}{Ignored; present for S3 \code{print} method compatibility.}
}
\value{
An S3 object of class \code{"stabl_fit"} containing:
\describe{
\item{\code{stabl_scores_}}{Numeric matrix (features \eqn{\times} lambdas)
of per-bootstrap selection frequencies (stability scores).}
\item{\code{stabl_scores_artificial_}}{Analogous matrix for artificial
features, or \code{NULL} when \code{artificial_type = NULL}.}
\item{\code{fitted_lambda_grid}}{The \code{data.frame} of lambda combinations
actually used.}
\item{\code{fdr_min_threshold_}}{FDP+-optimal stability threshold, or \code{NULL}.}
\item{\code{FDRs_}}{Numeric vector of FDP+ estimates per threshold, or \code{NULL}.}
\item{\code{min_fdr_}}{Minimum FDP+ achieved, or \code{NULL}.}
\item{\code{fdrs_table_}}{Per-lambda FDP+ matrix, or \code{NULL}.}
\item{\code{hard_threshold}}{As supplied.}
\item{\code{artificial_type}}{As supplied.}
\item{\code{artificial_provenance}}{List returned by
\code{\link[=make_artificial_features]{make_artificial_features()}} recording \code{actual_type}, all selected
generator modes, and the complete ordered fallback history. This is
the scientific source of truth for the generator used; or \code{NULL} when
\code{artificial_type = NULL}.}
\item{\code{artificial_proportion}}{As supplied.}
\item{\code{explore}}{As supplied.}
\item{\code{n_explore}}{As supplied.}
\item{\code{feature_names}}{Character vector of original feature names.}
\item{\code{n_features_in_}}{Integer number of original features.}
}
}
\description{
Performs the STABL bootstrap stability-selection procedure over a grid of
regularisation parameters, optionally injecting artificial features for
data-adaptive FDP+ threshold diagnostics. The fitting rule chooses the FDP+
minimizer; it is not a universal false-discovery guarantee. This is the R counterpart of
\code{Stabl.fit()} in the Python STABL library.
}
\section{Methods (by generic)}{
\itemize{
\item \code{print(stabl_fit)}: Print a concise summary of a fitted \code{stabl_fit} object;
invisibly returns \code{x}.

}}
\examples{
set.seed(42)
n <- 50L
p <- 10L
x <- matrix(
  rnorm(n * p),
  nrow = n,
  dimnames = list(paste0("s", seq_len(n)), paste0("f", seq_len(p)))
)
y <- setNames(0.8 * x[, 1] - 0.5 * x[, 2] + rnorm(n, sd = 0.3), rownames(x))
lambda_grid <- data.frame(lambda = seq(0.02, 0.30, length.out = 4L))

# Default lasso path
fit_lasso <- stabl_fit(
  x = x,
  y = y,
  lambda_grid = lambda_grid,
  n_bootstraps = 6L,
  artificial_type = "random_permutation",
  random_state = 1L
)
get_feature_names_out(fit_lasso)

# Adaptive lasso path
fit_adaptive <- stabl_fit(
  x = x,
  y = y,
  lambda_grid = lambda_grid,
  base_learner = "adaptive_lasso",
  adaptive_gamma = 1.0,
  adaptive_epsilon = 1e-6,
  n_bootstraps = 6L,
  artificial_type = "random_permutation",
  random_state = 2L
)
get_importances(fit_adaptive)

# Sparse-group lasso path (optional dependency)
if (requireNamespace("sparsegl", quietly = TRUE)) {
  feature_groups <- rep(seq_len(5L), each = 2L)
  fit_sgl <- stabl_fit(
    x = x,
    y = y,
    lambda_grid = lambda_grid,
    base_learner = "sparse_group_lasso",
    feature_groups = feature_groups,
    n_bootstraps = 5L,
    artificial_type = "random_permutation",
    random_state = 3L
  )
  get_support(fit_sgl)
}

# Multinomial path with three classes
y_multi <- setNames(
  factor(sample(c("A", "B", "C"), n, replace = TRUE)),
  rownames(x)
)
fit_multi <- suppressWarnings(stabl_fit(
  x = x,
  y = y_multi,
  lambda_grid = lambda_grid,
  family = "multinomial",
  n_bootstraps = 5L,
  artificial_type = "random_permutation",
  random_state = 4L
))
get_feature_names_out(fit_multi)
}
